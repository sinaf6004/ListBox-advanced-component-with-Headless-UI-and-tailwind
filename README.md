# Coponent explanation
here is a list box component that has been implemented by the react + tailwind + Headless UI.
## How to use it
It's easy, just clone this repository, go to the folder of the repository and install the dependencies using the command below:
```properties
npm i
```
After that, you can run the application using the following command:
```properties
npm run dev
```  
